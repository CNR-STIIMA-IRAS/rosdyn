#include "rdyn_core/primitives.h"
#include "rdyn_core/internal/primitives_impl.h"

namespace rdyn_core {

// As the _impl.h contains the definitions, we simply include it here.
// This makes sure that all template instantiations from _impl.h get compiled.

// Ensure that all the necessary instantiations or definitions are covered in _impl.h

// If you have specific non-template functions or classes that need explicit instantiation or
// separate implementation, you can define them here.

} // namespace rdyn_core
